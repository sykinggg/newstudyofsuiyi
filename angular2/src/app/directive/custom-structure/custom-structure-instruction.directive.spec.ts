import { CustomStructureInstructionDirective } from './custom-structure-instruction.directive';

describe('CustomStructureInstructionDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStructureInstructionDirective();
    expect(directive).toBeTruthy();
  });
});
