import { NumberResetDirective } from './number-reset.directive';

describe('NumberResetDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberResetDirective();
    expect(directive).toBeTruthy();
  });
});
