import { MySecDirectiveDirective } from './my-sec-directive.directive';

describe('MySecDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MySecDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
