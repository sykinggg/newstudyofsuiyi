import { CharLenDetectionDirective } from './char-len-detection.directive';

describe('CharLenDetectionDirective', () => {
  it('should create an instance', () => {
    const directive = new CharLenDetectionDirective();
    expect(directive).toBeTruthy();
  });
});
