import { AttrSecondDirectiveDirective } from './attr-second-directive.directive';

describe('AttrSecondDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrSecondDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
