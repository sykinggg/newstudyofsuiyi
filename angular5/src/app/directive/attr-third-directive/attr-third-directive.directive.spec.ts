import { AttrThirdDirectiveDirective } from './attr-third-directive.directive';

describe('AttrThirdDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrThirdDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
