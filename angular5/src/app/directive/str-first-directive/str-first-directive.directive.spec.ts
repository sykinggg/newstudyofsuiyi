import { StrFirstDirectiveDirective } from './str-first-directive.directive';

describe('StrFirstDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StrFirstDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
