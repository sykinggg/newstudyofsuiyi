import { PipeFirstPipe } from './pipe-first.pipe';

describe('PipeFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
