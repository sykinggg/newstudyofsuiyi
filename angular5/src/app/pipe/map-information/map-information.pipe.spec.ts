import { MapInformationPipe } from './map-information.pipe';

describe('MapInformationPipe', () => {
  it('create an instance', () => {
    const pipe = new MapInformationPipe();
    expect(pipe).toBeTruthy();
  });
});
